### Inheritance, composition, interfaces, tuples, enums

Typescript compiler
`npm i -g typescript ts-node`

Install all packeges
`npm i`

Start the project
`npm start`

To generate tsconfig.json
`tsc --init`

To generate package.json
`npm init -y`
